executable = `pwd`/test.sh
listFile = `pwd`/testList.list
outputDir = `pwd`/outputTest
maxJobs = 5
outputFile = job_test$jobIndex.root
maxAttempts=1

optTemplate = $outputFile

preExecute = source ~/env.sh

postExecute = ls
	pwd
	mv $outputFile $outputDir/$outputFile
